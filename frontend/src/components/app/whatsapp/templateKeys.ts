import { workspaceKeys } from '../../../api/queryKeys'

/** Query keys for message templates, shared by `TemplatePicker` and the templates area. */
export const templateKeys = workspaceKeys('templates')
