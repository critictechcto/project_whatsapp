import type { AreaMockHandlers } from '../registry/types'

/**
 * Mock handlers for `/api/v1/store/`, `/api/v1/payments/` and `/api/v1/seller-alerts/`.
 * Added once the commerce schema types are generated.
 */
export const handlers: AreaMockHandlers = []
